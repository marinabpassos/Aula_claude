# Só usa versões publicadas há pelo menos 7 dias (10080 minutos)
minimumReleaseAge: 10080
# Não rodamos os build scripts nativos (não usados neste CRUD).
allowBuilds:
  sharp: false
  unrs-resolver: false
